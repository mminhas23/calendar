<%= form_for(@tutor) do |f| %>
    <% if @tutor.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@tutor.errors.count, "error") %> prohibited this course from being saved:</h2>

            <ul>
                <% @tutor.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= f.label :firstName %><br/>
        <%= f.text_field :firstName %>
    </div>

    <div class="field">
        <%= f.label :lastName %><br/>
        <%= f.text_field :lastName %>
    </div>

    <div class="field">
        <%= f.label :email %><br/>
        <%= f.text_field :email %>
    </div>

    <div class="field">
        <%= f.label :dob %><br/>
        <%= f.text_field :dob %>
    </div>

    <div class="field">
        <%= f.label :contactNumber, 'Contact Number' %><br/>
        <%= f.text_field :contactNumber %>
    </div>
   
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>
